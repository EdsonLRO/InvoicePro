// =============================================================================
// InvoicePro — project settings
// -----------------------------------------------------------------------------
// Upload this file to your repository ONCE. You do not need to touch it again
// when index.html is updated, so your keys are never wiped by a new index.html.
//
// Both values below are PUBLIC-SAFE: the publishable key is designed to live in
// front-end code. Your data is protected by Row Level Security in the database,
// not by hiding this key. NEVER put the sb_secret_... (service_role) key here.
// =============================================================================
window.SUPABASE_URL = 'https://cuagwifetheefftleeup.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_sN8qt13wD8IZcDP222T6tA_ze5fxp-t';

// Public Cloudflare Turnstile site key. Leave blank until the production widget
// is provisioned and tested. The matching secret belongs only in Supabase Auth.
window.TURNSTILE_SITE_KEY = '';
window.STRIPE_LIVE_MODE = true;
