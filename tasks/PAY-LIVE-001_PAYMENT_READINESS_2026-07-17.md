# PAY-LIVE-001 Stripe Live-Payment Technical Readiness - 2026-07-17

Task ID: PAY-LIVE-001
Title: Harden and prepare the current invoice-payment flow for controlled live activation
Objective: Complete every safe repository, sandbox and deployment-preparation step needed to reduce live Stripe activation to explicit Owner/provider actions, without charging a real customer, exposing secrets, applying an unapproved production migration, publishing legal wording or launching publicly.
Priority: High
Status: Verification In Progress
Phase: Owner-approved production deployment complete; deployed negative-path sandbox replay passed; authenticated positive synthetic Checkout acceptance and live activation retained
Owner role: Payments Agent
Assigned specialists: Backend / Supabase Agent; Security Agent; QA Agent; Documentation Agent; Release Agent
Required reviewers: Payments Agent using Sol; Security Agent using Sol; QA Agent; Legal, Privacy and Regulatory Agent for the retained release block; Release Agent
Model/work mode: Sol for state-machine, financial-integrity and release decisions; Terra for scoped implementation and deterministic tests
Risk level: High
Affected files: `supabase/functions/create-stripe-checkout/`; `supabase/functions/create-stripe-refund/`; `supabase/functions/stripe-webhook/`; a new tracked migration; focused payment harnesses; `SECURITY_FINDINGS_LEDGER.md`; `PAYMENT_OPERATIONS_RUNBOOK.md`; `ROADMAP_EMAIL_PAYMENTS.md`; `APP_STATUS.md`; `PRODUCT_COMPLETION_LEDGER.md`; `RELEASE_READINESS.md`; this task record
Files or paths locked: Production migration/function locks released after the approved deployment; payment deployment evidence is locked on `codex/payment-deployment-evidence`; live-provider and customer-release targets remain approval-gated
Lock acquired: 2026-07-17
Expected release condition: focused implementation, sandbox-safe verification, independent-style payment/security review, deployment/rollback instructions and an explicit Owner-only activation list committed to a draft PR; no production migration or live action
Documents to read: `AGENTS.md`; `APP_STATUS.md`; `docs/INDEX.md`; full `AUTOMATION_MODEL_ORCHESTRATION.md`; full `AUTONOMOUS_EXECUTION_PERMISSION.md`; full `TALLYO_SECURITY_SAAS_MASTER_PLAN.md`; full `TALLYO_LEGAL_COMPLIANCE_AGENT.md`; `ROADMAP_EMAIL_PAYMENTS.md`; `PAYMENT_OPERATIONS_RUNBOOK.md`; `STRIPE_SANDBOX_TEST_EVIDENCE.md`; `RELEASE_READINESS.md`; affected source/migrations/tests; current official Stripe and Supabase sources
Documents explicitly not required: future SaaS subscription website/plans; unrelated Auth/MFA source; historical legal records except the current retained legal block; unrelated UI/PDF/PWA evidence
Dependencies: verified sandbox payment lifecycle; signed webhook and trusted Checkout binding; append-only audit constraints; Owner-approved product objective; current Supabase/Stripe accounts
Acceptance criteria: distinct simultaneous events cannot overwrite invoice payment/history state; invoice mutation and provider audit evidence commit atomically; duplicate and stale attempts are deterministic; Checkout creation uses provider idempotency; delayed, duplicate and out-of-order events fail safe; amounts/currencies/user/invoice/session binding remain enforced; no secret moves client-side; sandbox behaviour remains valid; live activation steps and rollback are exact and secret-free
Required tests: migration syntax/privilege review; focused Node payment harness; Edge Function format/type checks with frozen locks; duplicate, concurrent, stale, out-of-order, refund-failure, dispute and audit-failure cases; existing financial/security harnesses affected by source changes; `git diff --check`; secret/PII scan; required GitHub `verify`
Security boundary: preserve service-role custody, signed raw-body verification, five-minute signature tolerance, explicit test/live mode, tenant/user binding, trusted Tallyo-created Checkout evidence, amount/currency validation, append-only audit, provider authority and fail-closed errors
Privacy/legal materiality: High. The technical work processes invoice-recipient, payment and audit data. Current legal/privacy publication and real-customer blocks remain in force; this task does not accept or override them.
Payment impact: High. Repository and sandbox preparation only; no live key, payment, refund, payout, dispute action or customer link is authorised.
Production impact: The Owner approved the ordered test-mode production deployment on 2026-07-18. Migration `20260717165044` and the three matching Stripe functions are deployed; no live secret, live-mode, real-money, customer-communication or public-release action was authorised or performed.
Owner permission required: Satisfied for migration `20260717165044`, deployment of `create-stripe-checkout`, `create-stripe-refund` and `stripe-webhook`, and privacy-safe sandbox/readback checks. Separate approval remains required for live keys and webhook secret entry, `STRIPE_LIVE_MODE=true`, Stripe identity/banking/payout settings, a live endpoint, any real-money acceptance/refund, real customer link/email, protected high-risk merge where required, and public launch.
Legal disposition: `Approved with conditions` for internal technical/sandbox preparation; `Blocked` for real-customer/live use until current legal/privacy conditions are resolved or a policy-compliant recorded Owner disposition is permitted.
External review required: Existing one-off UK legal/privacy review remains required before real-customer onboarding under `LEGAL-OPS-001`; not required to write or locally test this technical hardening.
Legal review date: 2026-07-17; no override granted by the Owner's request to prioritise functionality.
Approval boundary: stop before secret reveal/entry, identity/banking verification, production provider/database changes, live-mode activation, spending, real-money tests, customer communication, legal publication or public launch
Implementation result: Merged source complete. Migration `20260717165044` adds an invoice mutation version trigger plus a service-role-only transaction that locks the invoice, rejects stale snapshots, records the Stripe audit event, and updates payment/history/status together. The webhook retries bounded stale results and reconciles current Stripe Refund state. Checkout creation uses deterministic provider idempotency and requires trusted-session audit evidence. Test/live key mismatches fail closed, live payments require the explicit server kill switch, and the frontend test notice requires a separate public live flag.
Review result: Payments/security review found and corrected timestamp-version collision and concurrent app-edit cases by moving to a trigger-maintained integer mutation version. QA review passed duplicate, distinct-event concurrency, stale retry, concurrent app-edit retry, privilege, out-of-order refund, idempotency, audit-failure, mode-mismatch and kill-switch checks. Release review confirms migration-before-function ordering and rollback/containment instructions. Legal/public release block remains unchanged.
Evidence: Existing `STRIPE_SANDBOX_TEST_EVIDENCE.md`; updated SEC-PAY-002; `tests/stripe-payment-integrity-harness.cjs`; `tests/stripe-payment-transaction-schema.sql`; `tests/stripe-payment-transaction-probe.sql`; isolated PostgreSQL 17.6 probe passed for atomic, duplicate, stale, concurrent app-edit retry and service-role-only execution; all nine focused CI harnesses and all ten frozen Edge checks passed; GitHub `Security checks` run `29602183325` passed on final PR head `e7dbd08`, PR #49 merged as `41a7bc9`, and deployment handoff PR #50 merged as `833052b`. On 2026-07-18, the CLI dry run named only `20260717165044`; the tracked migration applied successfully and remote history readback includes it. Post-migration metadata confirms invoice and audit RLS remain enabled, both append-only audit triggers remain present, `stripe_event_version` is a non-null bigint defaulting to zero, the invoice version trigger is active, the transaction RPC has an empty search path, `anon` and `authenticated` cannot execute it, and `service_role` can. The project remained `ACTIVE_HEALTHY`; the only advisor results were pre-existing informational notices. Checkout v6 and Refund v4 are active with JWT verification, while Webhook v11 remains JWT-exempt and verifies Stripe's signed raw body. Direct unauthenticated POSTs returned 401 for all three functions. A provider-generated sandbox asynchronous-payment-failure fixture reached Webhook v11 with HTTP 200; the aggregate Stripe audit count remained 60, confirming the unrelated fixture did not mutate application state. The public flag remains `false`. Neither available browser profile had an authenticated Tallyo session, so the positive synthetic Checkout path was not attempted and no credential was requested or inspected. Current official Supabase breaking-change and July 2026 changelog entries were rechecked on 2026-07-18; none changed this deployment result. No secret or customer data was inspected. Deno formatting was not made a new gate because all four affected function files already fail `deno fmt --check` on the prior baseline; no broad mechanical reformat was introduced.
Branch: `codex/payment-deployment-evidence` (source merged from `codex/live-payment-readiness`; deployment handoff merged from `codex/payment-deployment-handoff`)
Pull request: #49 (merged)
Commit: `97d7297` (`Harden Stripe live payment readiness`)
Merge: `41a7bc9` (`Merge PR #49: Harden Stripe live payment readiness`)
Deployment handoff merge: `833052b` (`Merge PR #50: Record production deployment handoff`)
Blocked reason: The migration, function deployment, schema/grant readback, negative HTTP checks and signed unrelated-event sandbox replay are complete. The final deployed positive synthetic Checkout acceptance needs an already authenticated browser session; neither available profile was signed in, and credentials must not be requested or inspected. Live-provider and real-customer actions remain separately blocked.
Next action: After the Owner signs in directly and leaves the app on the New Invoice screen, use synthetic-only data to create and complete one Stripe test-mode Checkout, verify one atomic invoice/audit mutation and duplicate replay, then record final evidence. Stop again before changing live secrets or mode, creating a live endpoint, publishing the live frontend flag, sending a real customer invoice, or initiating real money.
